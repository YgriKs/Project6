using System;

class Program {
    static void Main(string[] args) {
        int sum = 0;
		int a = Convert.ToInt32(Console.ReadLine());
		for (int i = 1; i <= a; i++)
		{
			sum += i;
		}
		Console.WriteLine(sum);
    }
}
